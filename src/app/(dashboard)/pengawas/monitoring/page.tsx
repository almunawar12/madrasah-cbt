import { MonitoringClient } from '../_components/monitoring-client';

export const metadata = { title: 'Monitoring · SantriExam' };

export default function MonitoringPage() {
  return <MonitoringClient />;
}
